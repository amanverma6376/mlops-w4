# Additional requirements for Week 8 Data Poisoning Assignment
# These extend the existing requirements.txt

# Core ML and data manipulation (likely already in requirements.txt)
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0

# MLflow for experiment tracking (likely already included)
mlflow>=2.0.0

# Visualization for analysis reports
matplotlib>=3.5.0
seaborn>=0.11.0

# Hyperparameter optimization (likely already included)
optuna>=3.0.0

# For statistical analysis and outlier detection
scipy>=1.7.0

# For robust model evaluation
joblib>=1.1.0

# Logging and utilities
tqdm>=4.60.0